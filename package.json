{
  "name": "opentasks",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --workspace=@opentasks/server --silent run dev",
    "typecheck": "npm --workspace=@opentasks/server --silent run typecheck"
  }
}
