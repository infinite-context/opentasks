{
  "name": "opentasks",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --workspace=@opentasks/server --silent run dev",
    "dev:web": "npm --workspace=@opentasks/web --silent run dev",
    "build:web": "npm --workspace=@opentasks/web --silent run build",
    "typecheck": "npm --workspace=@opentasks/server --silent run typecheck && npm --workspace=@opentasks/web --silent run typecheck"
  }
}
